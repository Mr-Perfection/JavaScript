# Intro to Node.js

## Getting started

```
$ mkdir sample
$ cd sample
$ npm init
$ npm install backbone -S # this will save the backbone into your node_modules and in your dependencies
$ npm install
```
